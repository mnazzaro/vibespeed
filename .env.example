# GitHub App Configuration
# Create a GitHub App at: https://github.com/settings/apps/new

# App Name (the URL slug of your GitHub App, e.g., "my-app-name")
GITHUB_APP_NAME=your-app-name

# App ID from your GitHub App settings
GITHUB_APP_ID=123456

# Client ID from your GitHub App settings
GITHUB_CLIENT_ID=Iv1.abc123def456

# Client Secret from your GitHub App settings
GITHUB_CLIENT_SECRET=your_client_secret_here

# Private Key - Either the path to the .pem file or the key contents (base64 encoded recommended)
# Option 1: Path to .pem file
GITHUB_PRIVATE_KEY_PATH=./private-key.pem
# Option 2: Base64 encoded private key (use: base64 -i private-key.pem | tr -d '\n')
# GITHUB_PRIVATE_KEY=LS0tLS1CRUdJTi...

# Webhook Secret (optional, for webhook validation)
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# OAuth Redirect URL (should match what's configured in GitHub App)
GITHUB_REDIRECT_URI=vibespeed://auth-callback

# Application Settings
APP_ENV=development
APP_DEBUG=true
APP_LOG_LEVEL=info

# Security Settings
# Generate a random string for session encryption
SESSION_SECRET=generate_a_random_string_here